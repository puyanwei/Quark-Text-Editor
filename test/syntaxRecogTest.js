var assert = require("assert");
var expect = require("chai").expect;
var syntaxRecog = require("../syntaxRecog");


describe("Menu", function() {
// test to be fixed
    xit('can change the class of the editor node', function() {
        currentFileName = "test.rb"
        syntaxRecog()
        expect(editor.className).to.eq("language-ruby")
    });

})
